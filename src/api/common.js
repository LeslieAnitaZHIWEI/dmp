/**
 * 一级目录地址
 * @types { string }
 **/
export const ROOT_URL = '/dmp'

/**
 * 设置大的请求时间
 * @types {number}
 **/
export const LONG_TIME = 1200000

/**
 * 分页的默认设置
 * @types { PageData }
 **/
export const pageData = {
  size: 20,
  currentPage: 1
}
